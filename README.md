# Défi OOTI

Pour lancer l'application, utiliser le script `ci/run.py` en lui fournissant le port d'écoute:

```
./ci/run.py <port>
```

Le script va automatiquement créer un venv et y installer les paquets (donc `pip` doit être installé).